namespace Siegebox.Persistence
{
    /// <summary>The persisted display state of a saved window, independent of any engine enum.</summary>
    public enum WindowDisplayState
    {
        Normal,
        Minimized,
        Maximized
    }
}
